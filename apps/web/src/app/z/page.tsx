import { RouteShellPage } from "@/components/route-shell";

export const metadata = {
  title: "Zones | AI-OSS.net",
};

export default function ZonesPage() {
  return <RouteShellPage specKey="zones" />;
}
